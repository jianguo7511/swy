<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apiyoo.anthorization.swy.mapper.CompanyMapper">

<select id="queryCompanyList" resultType="string">
        select  "— —" from  company where 1=1
        union
        select companyname from company where 1=1 and  delflag='0'
    </select>
        </mapper>